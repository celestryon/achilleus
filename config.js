// Configuration file for BanglaLeaks

// Placeholder for .onion URL - to be updated when SecureDrop is configured
const ONION_URL = 'banglaleaksxxxxxxxxxxxxxxxxxxxxxxxxxx.onion';

// Export for use in HTML
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { ONION_URL };
}
